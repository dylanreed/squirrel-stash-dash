[build]
  # No build command - serve pre-built files
  publish = "build/web"

# Headers for pygbag APK files
[[headers]]
  for = "/*.apk"
  [headers.values]
    Content-Type = "application/zip"
    Access-Control-Allow-Origin = "*"
    Cache-Control = "no-cache"
